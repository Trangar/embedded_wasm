[package]
name = "embedded_wasm"
version = "0.1.0"
edition = "2021"
license = "MIT"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
embedded_wasm_derive = { path = "derive" }

[features]
parse-globals = []
parse-memory = []
